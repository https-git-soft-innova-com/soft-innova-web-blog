---
import { getSiteSettings } from "emdash";
import {
	EmDashHead,
	EmDashBodyStart,
	EmDashBodyEnd,
} from "emdash/ui";
import { createPublicPageContext } from "emdash/page";
import { Font } from "astro:assets";
import { resolveBlogSiteIdentity } from "../utils/site-identity";
import WhatsAppFloat from "../components/WhatsAppFloat.astro";
import "../styles/theme.css";
import "../styles/article.css";

const SOFT_SITE = "https://soft-innova.com";

/** Enlaces navbar — URLs explícitas (sitio principal + blog). */
const NAV = {
	productos: "https://soft-innova.com/#productos",
	servicios: "https://soft-innova.com/#servicios",
	clientes: "https://soft-innova.com/#clientes",
	proposito: "https://soft-innova.com/proposito",
	blog: "/posts",
	reviews: "https://soft-innova.com/#reviews",
	conversemos: "https://soft-innova.com/#contacto",
} as const;

const FOOTER_SOCIAL = {
	facebook: "https://www.facebook.com/profile.php?id=61572338432440",
	instagram: "https://www.instagram.com/softech.innovacion/",
	linkedin: "https://www.linkedin.com/company/softech-innovacion-spa/",
} as const;

interface Props {
	title: string;
	pageTitle?: string | null;
	description?: string | null;
	image?: string | null;
	canonical?: string | null;
	robots?: string | null;
	type?: "website" | "article";
	publishedTime?: string | null;
	modifiedTime?: string | null;
	author?: string | null;
	/** Pass content reference for plugin page contributions on content pages */
	content?: { collection: string; id: string; slug?: string | null };
}

const {
	title,
	pageTitle,
	description,
	image,
	canonical,
	robots,
	type = "website",
	publishedTime,
	modifiedTime,
	author,
	content,
} = Astro.props;
const { siteTitle } = resolveBlogSiteIdentity(await getSiteSettings());
// If title already includes site title (from getSeoMeta), use as-is
const fullTitle = title.includes(siteTitle) ? title : `${title} — ${siteTitle}`;

const DEFAULT_OG_IMAGE = "https://soft-innova.com/images/iso-removebg-preview.png";
const resolvedImage = image ?? DEFAULT_OG_IMAGE;

// Build public page context for plugin contributions
// SEO data is passed here and rendered securely by EmDashHead
const pageCtx = createPublicPageContext({
	Astro,
	kind: content ? "content" : "custom",
	pageType: type,
	title: fullTitle,
	pageTitle: pageTitle ?? title,
	description,
	canonical,
	image: resolvedImage,
	content,
	seo: { ogImage: resolvedImage, robots },
	articleMeta: { publishedTime, modifiedTime, author },
	siteName: siteTitle,
});

---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<Font cssVariable="--font-sans" preload />
		<Font cssVariable="--font-mono" />
		<title>{fullTitle}</title>
		<EmDashHead page={pageCtx} />
		<script is:inline>
			// Apply theme immediately to prevent flash
			(function () {
				var c = document.cookie;
				var i = c.indexOf("theme=");
				var theme = i >= 0 ? c.slice(i + 6).split(";")[0] : null;
				if (theme === "dark" || theme === "light") {
					document.documentElement.classList.add(theme);
				} else if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
					document.documentElement.classList.add("dark");
				}
			})();
		</script>
	</head>
	<body>
		<EmDashBodyStart page={pageCtx} />
		<header class="site-header" data-site-header>
			<nav class="nav" aria-label="Principal">
				<a href={SOFT_SITE} class="nav-brand">
					<img
						src="/images/iso-removebg-preview.png"
						alt=""
						class="brand-logo-img"
						decoding="async"
					/>
					<div class="brand-text">
						<span class="brand-name" translate="no">
							Soft<span class="brand-accent">Innova</span>
						</span>
						<span class="brand-subtitle">IA · AUTOMATIZACIÓN</span>
					</div>
				</a>
				<div class="nav-links">
					<a href={NAV.productos}>Productos</a>
					<a href={NAV.servicios}>Servicios</a>
					<a href={NAV.clientes}>Clientes</a>
					<a href={NAV.proposito}>Propósito</a>
					<a href={NAV.blog}>Blog</a>
					<a href={NAV.reviews}>Reseñas</a>
				</div>
				<div class="nav-actions">
					<a class="nav-cta" href={NAV.conversemos}>
						Conversemos
					</a>
					{
						import.meta.env.DEV && (
							<a href="/_emdash/admin" class="nav-admin">
								Admin
							</a>
						)
					}
				</div>
			</nav>
		</header>

		<main>
			<slot />
		</main>

		<footer class="site-footer">
			<div class="footer-inner">
				<div class="footer-grid">
					<div class="footer-brand-col">
						<div class="footer-brand-row">
							<img
								src="/images/iso-removebg-preview.png"
								alt=""
								class="footer-logo-img"
								decoding="async"
							/>
							<div class="footer-wordmark" translate="no">
								Soft<span class="footer-wordmark-accent">Innova</span>
							</div>
						</div>
						<p class="footer-desc">
							Hacemos que la IA y la automatización funcionen en tu negocio.
							Productos listos, servicios a medida y un equipo que sí responde.
						</p>
						<div class="footer-social">
							<a
								href={FOOTER_SOCIAL.facebook}
								class="footer-social-link"
								target="_blank"
								rel="noopener noreferrer"
								aria-label="Facebook"
							>
								<svg class="footer-social-icon" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
									<path
										d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"
									></path>
								</svg>
							</a>
							<a
								href={FOOTER_SOCIAL.instagram}
								class="footer-social-link"
								target="_blank"
								rel="noopener noreferrer"
								aria-label="Instagram"
							>
								<svg
									class="footer-social-icon"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
									aria-hidden="true"
								>
									<rect width="20" height="20" x="2" y="2" rx="5" ry="5"></rect>
									<path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
									<line x1="17.5" x2="17.51" y1="6.5" y2="6.5"></line>
								</svg>
							</a>
							<a
								href={FOOTER_SOCIAL.linkedin}
								class="footer-social-link"
								target="_blank"
								rel="noopener noreferrer"
								aria-label="LinkedIn"
							>
								<svg
									class="footer-social-icon"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
									aria-hidden="true"
								>
									<path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6zM2 9h4v12H2z"></path>
									<circle cx="4" cy="4" r="2"></circle>
								</svg>
							</a>
						</div>
						<a
							href="https://www.cloudflare.com"
							target="_blank"
							rel="noopener"
							style="display:inline-block;margin-top:12px"
						>
							<img
								src="/images/cloudflare-badge.png"
								alt="Protected by Cloudflare"
								style="height:32px;opacity:0.8"
							/>
						</a>
					</div>
					<div class="footer-col">
						<h4 class="footer-heading">Productos</h4>
						<ul class="footer-links">
							<li><a href={`${SOFT_SITE}/#productos`}>Annie-AI</a></li>
							<li><a href={`${SOFT_SITE}/#productos`}>Vicky-AI</a></li>
							<li><a href={`${SOFT_SITE}/#productos`}>Ro-AI</a></li>
							<li><a href={`${SOFT_SITE}/#productos`}>Tito</a></li>
							<li><a href={`${SOFT_SITE}/#productos`}>Mikan</a></li>
							<li><a href={`${SOFT_SITE}/#productos`}>VAULT</a></li>
						</ul>
					</div>
					<div class="footer-col">
						<h4 class="footer-heading">Empresa</h4>
						<ul class="footer-links">
							<li><a href={`${SOFT_SITE}/#servicios`}>Servicios</a></li>
							<li><a href={NAV.blog}>Blog</a></li>
							<li><a href={`${SOFT_SITE}/#reviews`}>Reseñas</a></li>
							<li><a href={`${SOFT_SITE}/#contacto`}>Contacto</a></li>
						</ul>
					</div>
					<div class="footer-col">
						<h4 class="footer-heading">Contacto</h4>
						<ul class="footer-contact-list">
							<li>
								<a href="mailto:hola@soft-innova.com">hola@soft-innova.com</a>
							</li>
							<li>
								<a href="https://wa.me/56982637447" target="_blank" rel="noopener noreferrer">
									+56 9 8263 7447
								</a>
							</li>
							<li>Santiago, Chile</li>
						</ul>
					</div>
				</div>
				<div class="footer-bottom">
					<p class="footer-copyright">
						© 2026 SoftInnova. Hecho con foco en resultados.
					</p>
					<nav class="footer-legal" aria-label="Legal">
						<a href={`${SOFT_SITE}/privacidad`}>Privacidad</a>
						<a href={`${SOFT_SITE}/terminos`}>Términos</a>
						<a href={`${SOFT_SITE}/propiedad-intelectual`}>Propiedad intelectual</a>
					</nav>
				</div>
			</div>
		</footer>

		<style is:global>
			/*
			 * Establish layer order: "base" has lower priority than unlayered
			 * styles, so theme.css overrides always win regardless of source
			 * order in the bundled CSS.
			 */
			@layer base;

			@layer base {
				*:where(:not([class*="emdash"]):not([class*="ec-"])),
				*:where(:not([class*="emdash"]):not([class*="ec-"]))::before,
				*:where(:not([class*="emdash"]):not([class*="ec-"]))::after {
					box-sizing: border-box;
				}

				body,
				h1,
				h2,
				h3,
				h4,
				h5,
				h6,
				p,
				ul,
				ol,
				figure,
				blockquote,
				dl,
				dd {
					margin: 0;
				}

				ul,
				ol {
					padding: 0;
				}

				:root {
					/* Colors - Light mode (default) */
					--color-bg: #ffffff;
					--color-bg-subtle: #fafafa;
					--color-text: #1a1a1a;
					--color-text-secondary: #525252;
					--color-muted: #8b8b8b;
					--color-border: #e5e5e5;
					--color-border-subtle: #f0f0f0;
					--color-surface: #f7f7f7;
					--color-accent: #0066cc;
					--color-accent-hover: #0052a3;
					--color-on-accent: white;
					--color-accent-ring: color-mix(
						in srgb,
						var(--color-accent) 25%,
						transparent
					);

					/* Type scale - more refined */
					--font-size-xs: 0.8125rem;
					--font-size-sm: 0.875rem;
					--font-size-base: 1rem;
					--font-size-lg: 1.125rem;
					--font-size-xl: 1.25rem;
					--font-size-2xl: 1.5rem;
					--font-size-3xl: 2rem;
					--font-size-4xl: 2.5rem;
					--font-size-5xl: 3.5rem;

					/* Line heights */
					--leading-tight: 1.15;
					--leading-snug: 1.3;
					--leading-normal: 1.5;
					--leading-relaxed: 1.7;

					/* Spacing - more generous */
					--spacing-1: 0.25rem;
					--spacing-2: 0.5rem;
					--spacing-3: 0.75rem;
					--spacing-4: 1rem;
					--spacing-5: 1.25rem;
					--spacing-6: 1.5rem;
					--spacing-8: 2rem;
					--spacing-10: 2.5rem;
					--spacing-12: 3rem;
					--spacing-16: 4rem;
					--spacing-20: 5rem;
					--spacing-24: 6rem;

					/* Legacy spacing aliases */
					--spacing-xs: var(--spacing-1);
					--spacing-sm: var(--spacing-2);
					--spacing-md: var(--spacing-4);
					--spacing-lg: var(--spacing-6);
					--spacing-xl: var(--spacing-8);
					--spacing-2xl: var(--spacing-12);
					--spacing-3xl: var(--spacing-16);

					/* Layout - wider for three-column */
					--content-width: 680px;
					--wide-width: 1200px;
					--max-width: var(--content-width);
					--gutter-width: 200px;
					--radius: 4px;
					--radius-lg: 8px;

					/* Transitions */
					--transition-fast: 120ms ease;
					--transition-base: 180ms ease;

					/* Nav */
					--nav-height: 72px;

					/* Article layout */
					--meta-col-width: 180px;

					/* Avatar sizes */
					--avatar-size-xs: 18px;
					--avatar-size-sm: 20px;
					--avatar-size-md: 24px;
					--avatar-size-lg: 32px;

					/* Letter spacing */
					--tracking-tight: -0.03em;
					--tracking-snug: -0.02em;
					--tracking-wide: 0.06em;
					--tracking-wider: 0.08em;

					/* Tag pill */
					--tag-padding-y: 2px;

					/* Shadows */
					--shadow-dropdown: 0 8px 30px rgba(0, 0, 0, 0.12);
					--shadow-btn-active: 0 1px 2px rgba(0, 0, 0, 0.05);
				}

				/* Dark mode via system preference (when no explicit class) */
				@media (prefers-color-scheme: dark) {
					:root:not(.light) {
						--color-bg: #0d0d0d;
						--color-bg-subtle: #141414;
						--color-text: #ededed;
						--color-text-secondary: #a0a0a0;
						--color-muted: #6b6b6b;
						--color-border: #2a2a2a;
						--color-border-subtle: #1f1f1f;
						--color-surface: #181818;
						--color-accent: #4d9fff;
						--color-accent-hover: #6eb0ff;
					}
				}

				/* Explicit dark mode */
				:root.dark {
					--color-bg: #0d0d0d;
					--color-bg-subtle: #141414;
					--color-text: #ededed;
					--color-text-secondary: #a0a0a0;
					--color-muted: #6b6b6b;
					--color-border: #2a2a2a;
					--color-border-subtle: #1f1f1f;
					--color-surface: #181818;
					--color-accent: #4d9fff;
					--color-accent-hover: #6eb0ff;
				}

				html {
					scroll-behavior: smooth;
				}

				body {
					font-family: Inter, system-ui, sans-serif;
					font-size: var(--font-size-base);
					line-height: var(--leading-relaxed);
					color: var(--color-text);
					background: var(--color-bg);
					-webkit-font-smoothing: antialiased;
					-moz-osx-font-smoothing: grayscale;
					min-height: 100vh;
				}

				a:where(:not([class*="emdash"]):not([class*="ec-"])) {
					color: var(--color-accent);
					text-decoration: none;
					transition: color var(--transition-fast);
				}

				a:where(:not([class*="emdash"]):not([class*="ec-"])):hover {
					color: var(--color-accent-hover);
				}

				img {
					max-width: 100%;
					height: auto;
					display: block;
				}

				h1,
				h2,
				h3,
				h4,
				h5,
				h6 {
					font-family: Inter, system-ui, sans-serif;
					line-height: var(--leading-tight);
					font-weight: 600;
					letter-spacing: var(--tracking-snug);
				}

				h1 {
					font-weight: 700;
					letter-spacing: var(--tracking-tight);
				}

				::selection {
					background: var(--color-accent);
					color: white;
				}
			}
		</style>

		<style>
			.site-header {
				background: rgba(11, 31, 58, 0.85);
				backdrop-filter: blur(12px);
				-webkit-backdrop-filter: blur(12px);
				border-bottom: 1px solid rgba(255, 255, 255, 0.08);
				position: sticky;
				top: 0;
				z-index: 100;
			}

			.site-header.is-scrolled {
				background: rgba(11, 31, 58, 0.92);
				box-shadow: 0 2px 12px rgba(0, 0, 0, 0.25);
				border-bottom-color: rgba(255, 255, 255, 0.08);
			}

			:root.dark .site-header.is-scrolled {
				background: rgba(11, 31, 58, 0.92);
				box-shadow: 0 2px 12px rgba(0, 0, 0, 0.35);
				border-bottom-color: rgba(255, 255, 255, 0.08);
			}

			.nav {
				display: grid;
				grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
				align-items: center;
				column-gap: var(--spacing-4);
				max-width: var(--wide-width);
				margin: 0 auto;
				padding: var(--spacing-4) var(--spacing-6);
				min-height: var(--nav-height);
				position: relative;
			}

			.nav-brand {
				justify-self: start;
				display: flex;
				align-items: center;
				gap: var(--spacing-3);
				text-decoration: none;
				color: #ffffff;
				letter-spacing: var(--tracking-snug);
			}

			.nav-brand:hover .brand-accent {
				color: var(--color-accent-hover);
			}

			.brand-logo-img {
				display: block;
				height: 40px;
				width: auto;
				flex-shrink: 0;
			}

			.brand-text {
				display: flex;
				flex-direction: column;
				gap: 2px;
				line-height: 1.15;
			}

			.brand-name {
				font-size: var(--font-size-lg);
				font-weight: 700;
				color: white;
			}

			.brand-accent {
				color: var(--color-accent);
				transition: color var(--transition-fast);
			}

			.brand-subtitle {
				font-size: 10px;
				font-weight: 500;
				color: rgba(255, 255, 255, 0.55);
				letter-spacing: 0.08em;
			}

			:root.dark .brand-subtitle {
				color: rgba(255, 255, 255, 0.55);
			}

			@media (prefers-reduced-motion: reduce) {
				.site-header {
					transition: none;
				}
			}

			.nav-cta {
				display: inline-flex;
				align-items: center;
				justify-content: center;
				background: linear-gradient(135deg, #E07020, #F59B20);
				color: white !important;
				padding: 10px 22px;
				border-radius: 9999px;
				font-weight: 700;
				font-size: 0.875rem;
				text-decoration: none;
				transition: opacity 0.2s;
				white-space: nowrap;
				border: none;
			}

			.nav-cta:hover {
				opacity: 0.9;
				color: white !important;
			}

			.nav-cta:focus-visible {
				outline: 2px solid #ffffff;
				outline-offset: 3px;
			}

			.nav-links {
				justify-self: center;
				display: flex;
				gap: var(--spacing-5);
				font-family: Inter, system-ui, sans-serif;
			}

			.nav-actions {
				justify-self: end;
				display: flex;
				align-items: center;
				gap: var(--spacing-6);
			}

			.nav-links a {
				text-decoration: none;
				font-size: 15px;
				font-weight: 500;
				color: rgba(255, 255, 255, 0.85);
				transition: color 200ms ease;
			}

			.nav-links a:hover {
				color: white;
			}

			:root.dark .nav-links a {
				color: rgba(255, 255, 255, 0.85);
			}

			:root.dark .nav-links a:hover {
				color: white;
			}

			.nav-links a:focus-visible {
				outline: 2px solid var(--color-accent);
				outline-offset: 3px;
				border-radius: var(--radius);
			}

			.nav-admin {
				font-size: 15px;
				font-weight: 500;
				font-family: Inter, system-ui, sans-serif;
				color: var(--color-text-secondary);
				text-decoration: none;
				transition: color var(--transition-fast);
			}

			.nav-admin:hover {
				color: var(--color-text);
			}

			main {
				min-height: calc(100vh - var(--nav-height) - 300px);
			}

			/* Footer — alineado con site/Footer.astro (soft-innova.com) */
			.site-footer {
				background: #0f172a;
				color: #fff;
				padding: 5rem 0 2.5rem;
				border-top: 1px solid rgba(255, 255, 255, 0.05);
			}

			.footer-inner {
				max-width: var(--wide-width);
				margin: 0 auto;
				padding: 0 1.25rem;
			}

			.footer-grid {
				display: grid;
				grid-template-columns: repeat(12, minmax(0, 1fr));
				gap: 2.5rem;
			}

			.footer-brand-col {
				min-width: 0;
				grid-column: 1 / -1;
			}

			.footer-brand-row {
				display: flex;
				align-items: center;
				gap: 0.5rem;
				margin-bottom: 1.25rem;
			}

			.site-footer .footer-logo-img {
				display: block;
				height: 48px;
				width: auto;
				flex-shrink: 0;
			}

			.footer-wordmark {
				font-size: var(--font-size-2xl);
				font-weight: 700;
				color: #fff;
				letter-spacing: var(--tracking-snug);
			}

			.footer-wordmark-accent {
				color: #f5a623;
			}

			.footer-desc {
				font-size: var(--font-size-sm);
				line-height: var(--leading-relaxed);
				color: rgba(255, 255, 255, 0.65);
				max-width: 24rem;
			}

			.footer-social {
				display: flex;
				gap: 0.75rem;
				margin-top: 1.5rem;
			}

			.footer-social-link {
				display: flex;
				align-items: center;
				justify-content: center;
				width: 2.5rem;
				height: 2.5rem;
				border-radius: 9999px;
				background: rgba(255, 255, 255, 0.05);
				color: #fff;
				text-decoration: none;
				transition: background-color 200ms ease;
			}

			.footer-social-link:hover {
				background: #f5a623;
				color: #fff;
			}

			.footer-social-icon {
				width: 18px;
				height: 18px;
			}

			.footer-col {
				min-width: 0;
				grid-column: 1 / -1;
			}

			.site-footer .footer-heading {
				font-size: var(--font-size-sm);
				font-weight: 700;
				text-transform: uppercase;
				letter-spacing: 0.05em;
				color: #fff;
				margin-bottom: 1rem;
			}

			.site-footer .footer-links {
				list-style: none;
				margin: 0;
				padding: 0;
			}

			.site-footer .footer-links li {
				margin-bottom: 0.5rem;
			}

			.site-footer .footer-links a {
				font-size: var(--font-size-sm);
				color: rgba(255, 255, 255, 0.65);
				text-decoration: none;
				transition: color 200ms ease;
			}

			.site-footer .footer-links a:hover {
				color: #f5a623;
			}

			.footer-contact-list {
				list-style: none;
				margin: 0;
				padding: 0;
				font-size: var(--font-size-sm);
				color: rgba(255, 255, 255, 0.65);
				line-height: var(--leading-relaxed);
			}

			.footer-contact-list li {
				margin-bottom: 0.5rem;
			}

			.site-footer .footer-contact-list a {
				color: rgba(255, 255, 255, 0.65);
				text-decoration: none;
				transition: color 200ms ease;
			}

			.site-footer .footer-contact-list a:hover {
				color: #f5a623;
			}

			.footer-bottom {
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: space-between;
				gap: 0.75rem;
				margin-top: 3.5rem;
				padding-top: 1.5rem;
				border-top: 1px solid rgba(255, 255, 255, 0.1);
			}

			.footer-copyright {
				font-size: 0.75rem;
				color: rgba(255, 255, 255, 0.5);
				margin: 0;
				text-align: center;
			}

			.footer-legal {
				display: flex;
				flex-wrap: wrap;
				gap: 1.25rem 1.25rem;
				align-items: center;
				justify-content: center;
			}

			.site-footer .footer-legal a {
				font-size: 0.75rem;
				color: rgba(255, 255, 255, 0.5);
				text-decoration: none;
				transition: color 200ms ease;
			}

			.site-footer .footer-legal a:hover {
				color: #fff;
			}

			@media (min-width: 768px) {
				.footer-brand-col {
					grid-column: 1 / span 5;
				}

				.footer-grid > .footer-col:nth-child(2) {
					grid-column: span 2;
				}

				.footer-grid > .footer-col:nth-child(3) {
					grid-column: span 2;
				}

				.footer-grid > .footer-col:nth-child(4) {
					grid-column: span 3;
				}

				.footer-bottom {
					flex-direction: row;
					align-items: center;
				}

				.footer-copyright {
					text-align: left;
				}

				.footer-legal {
					justify-content: flex-end;
				}
			}

			@media (max-width: 640px) {
				.nav {
					display: flex;
					flex-direction: column;
					align-items: stretch;
					position: relative;
					min-height: 0;
					gap: var(--spacing-2);
					padding: var(--spacing-3) var(--spacing-4);
				}

				.brand-name {
					font-size: var(--font-size-base);
				}

				.brand-logo-img {
					height: 40px;
				}

				.nav-links {
					order: 2;
					width: 100%;
					justify-content: center;
					flex-wrap: wrap;
					column-gap: var(--spacing-3);
					row-gap: var(--spacing-1);
				}

				.nav-actions {
					order: 3;
					width: 100%;
					flex-direction: column;
					align-items: stretch;
					gap: var(--spacing-2);
				}

				.nav-cta {
					width: 100%;
					justify-content: center;
					min-height: 48px;
				}

				.nav-admin {
					position: absolute;
					right: var(--spacing-4);
					top: var(--spacing-3);
					font-size: var(--font-size-xs);
				}

				.footer-grid {
					grid-template-columns: 1fr 1fr;
					gap: var(--spacing-8);
				}

				.footer-brand-col {
					grid-column: 1 / -1;
				}

				.footer-col {
					grid-column: span 1;
				}

				.footer-bottom {
					align-items: center;
					text-align: center;
				}

				.footer-copyright {
					text-align: center;
				}

				.footer-legal {
					justify-content: center;
				}
			}
		</style>

		<script>
			const header = document.querySelector("[data-site-header]");
			function syncHeaderScroll() {
				if (!header) return;
				header.classList.toggle("is-scrolled", window.scrollY > 20);
			}
			syncHeaderScroll();
			window.addEventListener("scroll", syncHeaderScroll, { passive: true });
		</script>

		<WhatsAppFloat />

		<EmDashBodyEnd page={pageCtx} />
	</body>
</html>
